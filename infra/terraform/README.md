# Terraform

This directory contains the AWS infrastructure foundation for Email Automation Engine.

Phase 1 only defines provider constraints, product-neutral variables, and a reusable naming prefix. Queue, Lambda, SES, and IAM modules will be added in later phases.
